# Local Phase 1 — copy values into shell or use as reference for docker compose
# Grafana is disabled; Oracle Cloud is a later step.

GRAFANA_DISABLED=true
MCP_ENABLED=false
WATCHDOG_ENABLED=false

DB_HOST=postgres
DB_PORT=5432
DB_USER=blackbox_user
DB_PASSWORD=blackbox_local_pass
DB_NAME=am_blackbox
