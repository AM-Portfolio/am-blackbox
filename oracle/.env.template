# AM Blackbox Production Environment Variables
# Copy this file to .env on your Oracle VM before running deploy.sh

# 1. Database Configuration
DB_PASSWORD=YOUR_SECURE_DATABASE_PASSWORD

# 2. Grafana Cloud Configuration (Pre-filled from your portal)
GRAFANA_API_TOKEN=YOUR_GRAFANA_API_TOKEN
GRAFANA_PROMETHEUS_URL=https://prometheus-prod-43-prod-ap-south-1.grafana.net/api/prom/push
GRAFANA_PROMETHEUS_USERNAME=3603868
GRAFANA_LOKI_URL=https://logs-prod-028.grafana.net/loki/api/v1/push
GRAFANA_LOKI_USERNAME=1797605

# 3. Zoho Cliq Webhook (Optional, for notifications)
ZOHO_CLIQ_WEBHOOK_URL=YOUR_ZOHO_WEBHOOK_URL

# 4. MCP Credentials (From asrax.prod.env)
AM_MCP_CLIENT_ID=YOUR_MCP_CLIENT_ID
AM_MCP_CLIENT_SECRET=YOUR_MCP_CLIENT_SECRET
